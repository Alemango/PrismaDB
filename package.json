{
  "name": "prismadb",
  "version": "1.0.0",
  "description": "Conexion a DB",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "alemango",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.18.1",
    "prisma": "^4.0.0"
  },
  "dependencies": {
    "@prisma/client": "^4.0.0"
  }
}
